const name = 'Batman';
const age = '30';

// SIMPLE IF-ELSE

if(age==30 && name=='Batman'){  
    console.log(`${name} is the SuperHero`);
}
else
    console.log('Not Batman');

// JAVASCRIPT STYLED IF-ELSE

if(age===30 && name==='Batman'){  
    console.log(`${name} is the SuperHero`);
}
else
    console.log('Not Batman');
    